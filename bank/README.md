# Local Bank Workspace

This directory is intentionally left blank in git.

Create your personal working bank here with:

```bash
python3 scripts/init_bank.py --target bank
```

The generated `bank/` contents stay local to you and are ignored by git. The public sample bank lives in `examples/sample_bank/`.

